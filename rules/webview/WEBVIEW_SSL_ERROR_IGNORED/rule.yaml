id: WEBVIEW_SSL_ERROR_IGNORED
severity: high
version: 1.0.0
protocol_version: 1.0.0
component: webview
evidence_output: L2
builtin: true
entrypoint: detect.py
android_api: "1-36"
limitations: "onReceivedSslError 内调用 handler.proceed() 绕过证书校验，MITM 可注入任意内容。P7/E6 起按花括号配对提取完整方法体——嵌套块（if/try）之后放行的常见写法可检（旧正则停在首个 '}' 漏检）；WebViewClient 是否实际安装（setWebViewClient 关联）由 AI 复核确认。"
