# 评审文档：T2.6 实施方案与验收方案

> **任务编号**：T2.6
> **评审日期**：2026-08-22
> **评审对象**：`docs/analysis/2026-08-22-t2-6-implementation-plan.md`、`docs/analysis/2026-08-22-t2-6-acceptance-plan.md`
> **评审模型**：deepseek-v4-flash（独立子 agent，代码级核验）
> **评审者**：独立评审子 agent（code-explorer）
> **状态**：已闭合（第 1 轮处置完成，无开放决策项）

---

## 1. 评审结论摘要

方案与 T0.1 冻结 schema、大纲 §2.2/§2.5、代码锚点高度吻合（D3 path 消费链核实成立——method_id 前缀与 files.path 同源、精确等值匹配可命中）。主要缺口：行号粒度论证不完整（模型系统性 off-by-offset 时三档统计失真，"保守失败可接受"不能覆盖）；§3.3 集成示意引用作用域外不存在的 manifest 变量；§3.6 授权阻断未实现标"一致"名不副实。修订后实施。

## 2. 问题清单与处置（第 1 轮全采纳）

| 编号 | 严重度 | 问题 | 处置 |
|---|---|---|---|
| R-1 | 中 | 行号粒度：模型见 body 无行号纯切片（换算绝对行责任在模型），系统性偏差致 validated 系统性偏低 | **采纳**：校验器统计 line_mismatch_hops（跳失败但 (from,to) 存在唯一 resolved 边——仅行号不匹配）写入 notes/summary 诊断；prompt 绝对行号说明**移交 T2.5 协议迭代记录**（协议已冻结不动） |
| R-2 | 中 | `_run_explorer_stage` 签名无 manifest，§3.3 示意卡死 | **采纳**：签名扩展 manifest 参数（调用点 L281 作用域可传；guard_manifest_facts 先例 L200-204 复用） |
| R-3 | 中 | "授权阻断"能力不存在（guard_verifier 仅 debuggable）却标"一致" | **采纳**：对照表改"部分（授权阻断延后）"；D3 记录边界 |
| R-4 | 低 | summary 计数字段名未冻结 | **采纳**：`validation_counts` 嵌套结构冻结 |
| R-5 | 低 | resolved 边语义边界（仅同类/同包唯一解析）——validated 实际语义="同包唯一解析链" | **采纳**：风险表补注 |
| R-6 | 低 | 变更清单漏 test_explorer.py vs §3.5-7 表述矛盾 | **采纳**：直调测试并入 test_explorer_validation.py |
| R-7 | 低 | D2 边界无负例 | **采纳**：补 N-5（伪 custom sink → status 正常判定 + 标志 false） |
| R-8 | 低 | _write_candidates 跨对象私有调用 | **采纳**：explorer.py 加公有 `save_candidates` 包装 |

## 3. 认可项

- D3 path 消费链成立（同源+等值匹配+prompt"不得拼造"保障）；call_sites 列名/值域/resolve 语义假设无误；插入点正确（explore_all 后 finally 前）；保守失败方向安全（假阴性只降档不丢候选）；A-4 fixture 可改造（test_guard_verifier 行布局）；N-4 与既有语义一致；边界检查全部通过（D2 裁剪论证成立——SINK_PATTERNS 仅存规则侧，backend 不 import 是 T2.1 原则；**T2.7 需先解决 taxonomy 的 backend 侧落点否则该边界顺延**——已记录为 T2.7 前置观察项）。

## 4. 移交记录

- prompt 绝对行号说明（R-1）：属 T2.5 协议范畴，以本评审记录为准——T2.5 协议迭代（explorer/1.1.0）时补"call_site_line 需从方法体行号换算为文件绝对行号"的硬约束；
- taxonomy backend 侧落点（评审边界检查）：T2.7 归一化设计时评估（custom_sink_proposal 的判定数据源）。
