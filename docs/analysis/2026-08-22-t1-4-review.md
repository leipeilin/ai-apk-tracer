# 评审文档：T1.4 实施方案与验收方案

> **任务编号**：T1.4
> **评审日期**：2026-08-22
> **评审对象**：`docs/analysis/2026-08-22-t1-4-implementation-plan.md`、`docs/analysis/2026-08-22-t1-4-acceptance-plan.md`
> **评审模型**：deepseek-v4-flash（独立子 agent）
> **评审者**：独立评审子 agent（code-explorer）
> **状态**：已闭合（第 1 轮处置完成，无开放决策项）

---

## 1. 评审结论摘要

两份方案主干成立：D1-D6 决策的事实基础经代码逐条核实无误（assets 表 NOT NULL、AppError 可直连实例化、create_run 授权先例、BackgroundTasks 传 async 方法先例），测试分层（API 层验协议、编排归 test_batch.py）正确。两处中等问题：batch 响应残留 `assets_json` 原始列（双份字段固化进 API 契约）、测试未显式隔离 `assets.data_root`（默认工作区相对路径，全量回归将真实 APK 副本写入工作区）。修订后实施。

## 2. 问题清单

| 编号 | 严重度 | 问题描述 | 证据 | 修订建议 |
|---|---|---|---|---|
| R-1 | 中 | batches 端点透传 get_batch 返回值，`assets_json`（原始 JSON 字符串）与解析后 `assets` 双份字段进入 API 契约 | batch.py:154-155 | 增设 `_public_batch` 剔除 assets_json；A-6/A-9 补断言 |
| R-2 | 中 | 测试 Settings 未指定 assets.data_root，默认 `.ai-apk-tracer/assets` 工作区相对路径，全量回归写真实 APK 副本入工作区 | 实施稿 §3.4、config.py:219 | 测试工厂显式 `data_root=tmp_path/"assets"` |
| R-3 | 低 | 新公开上传端点缺恶意文件名用例（INVALID_APK_FILENAME 全测试 0 命中） | registry.py:77-78 | A-5 增补路径穿越文件名 → 422 |
| R-4 | 低 | D1 引证不全（未回应方案 L138"给定 package list 批量创建 run"）；验收稿 §1 不自包含 | 方案 L138 | D1 补辩护（asset_ids 子集已覆盖"按包批量"语义）；验收稿补范围声明 |
| R-5 | 低 | A-12 "942+" 下界表述松 | 验收稿 A-12 | 写死 952（942+10） |
| R-6 | 低 | AppError import 未入变更清单；status_code 宜用 status enum | routes.py:17/91 | 变更摘要补 import；伪码统一 status enum |

## 3. 认可项

- D2 与 create_run 完全同构；batches 用 JSON body 合理（对齐 ReviewRequest 先例）。
- D3 成立：AppError 直连 + 统一 handler 零成本；503 + ASSETS_DISABLED 供前端分支；不复用 DependencyError 正确。
- D4 准确（import 同步 201 / batches 异步 202 与 create_run 一致）。
- run_batch no-op 替换正当（test_batch.py 已覆盖编排；add_task 传 async 有直接先例）。
- UploadFile.file 与 registry.register 流式分块兼容（同 create_run 先例）；A-1 默认门禁回归必要；D1 事实核实无误；D6 与领域模块无门禁决策链一致。

## 4. 处置记录（主 agent，第 1 轮）

| 编号 | 处置 | 理由 | 修订动作 | 修订后位置 |
|---|---|---|---|---|
| R-1 | 采纳 | API 契约去重 | routes 增 `_public_batch`（剔除 assets_json）；A-6/A-9 补断言响应无 assets_json | 实施稿 §3.2、验收稿 A-6/A-9 |
| R-2 | 采纳 | 测试隔离（防工作区污染） | 测试工厂 `assets=AssetsSettings(enabled=True, data_root=tmp_path/"assets")` | 实施稿 §3.4 |
| R-3 | 采纳 | 公开端点安全负例 | A-5 补 `filename="../../evil.apk"` → 422 INVALID_APK_FILENAME | 验收稿 A-5 |
| R-4 | 采纳 | 论证完整 + 自包含 | D1 补 L138 辩护（"按包批量"由 asset_ids 子集承载——按包名筛选资产由前端/T1.5 组合，API 按资产 id 批量）；验收稿 §1 补"仅 APK 导入（D1）" | 实施稿 D1、验收稿 §1 |
| R-5 | 采纳 | 确定性验收 | A-12 = 952 | 验收稿 A-12 |
| R-6 | 采纳 | 实施完备性 | 变更清单补 AppError import；`status.HTTP_201_CREATED`/`HTTP_202_ACCEPTED` enum | 实施稿 §3.1/§3.2 |

## 5. 开放决策项

无。6 项意见第 1 轮全部采纳，进入实施。

## 6. 受控越界标注（经评审）

- `main.py` 组装超出实施计划 T1.4 行文件清单（仅 routes.py/models.py）——必要增量（app.state 注入），本评审确认受控（同 T1.3 评审 §6 先例）。

## 7. 遗留给 T1.5 的预判项（记录）

- `GET /api/batches`（列表）大概率 T1.5 需要（前端刷新后无批次发现途径）——届时按需求补；
- 前端按 `ASSETS_DISABLED`（503）与 `ASSET_ALREADY_REGISTERED`（409, details.asset_id）分支提示。
