# 评审文档：T0.7 实施方案与验收方案

> **任务编号**：T0.7
> **评审日期**：2026-08-22
> **评审对象**：`docs/analysis/2026-08-22-t0-7-implementation-plan.md`、`docs/analysis/2026-08-22-t0-7-acceptance-plan.md`
> **评审模型**：deepseek-v4-flash（独立子 agent）
> **评审者**：独立评审子 agent（code-explorer）
> **状态**：已闭合（第 1 轮处置完成，无开放决策项）

---

## 1. 评审结论摘要

六段字段与方案 §5.5 草案逐一对齐，默认值整体保守（enabled=False、auto_promote=false、0=沿用/不限语义正确），安全默认到位。主要风险集中在协议版本与 registry 未注册的信息不对称（explorer 与 verify 两处，方案仅声明了 verify）、assets.data_root 缺工作区解析助手、同名 `max_concurrent_runs` 层级语义易混。修订后通过。

## 2. 问题清单

| 编号 | 严重度 | 问题描述 | 证据 | 修订建议 |
|---|---|---|---|---|
| R-1 | 高 | `explorer/1.0.0` 与 `verify/1.0.0` 均未在 registry 注册，方案仅声明 verify 属 T0.9；explorer 未归因（实属 T2.5），存在"已声明版本误当已注册版本硬解析"风险 | `prompts/registry.yaml` 无 explorer/verify；实施计划 T2.5 才新增 explorer prompt | §3.6 补 explorer 先声明后注册说明 + "注册前不得运行时解析"守卫说明 |
| R-2 | 中 | `assets.data_root` 为 Path 但无工作区解析助手（仅 `storage.data_root` 有 `resolved_data_root()`） | `config.py:219-222` | 补 `resolved_assets_data_root()`（复用 `_resolve_workspace_path`） |
| R-3 | 中 | schema 中 assets.data_root（Path）类型表达未明确 | `config.schema.json:47`（storage 用 string+minLength） | schema 复用 string+minLength，测试断言 string 类型 |
| R-4 | 中 | assets 与 batch 同名 `max_concurrent_runs`（均 2）语义易混 | 实施计划 §3.2/§3.3 | 保留字段名（对齐 §5.5 草案），但 description 写明层级（资产扫描并发 vs 批内 run 并发） |
| R-5 | 中 | 测试未覆盖 schema 顶层 required 与 Path 类型、registry 一致性 | 验收 A-5 | 补 N-5（data_root 为 string）、N-6（顶层 required 含新 6 段）；prompt_version 与 registry 一致性守卫测试 |
| R-6 | 低 | N-2 反例未配正向断言（batch 缺省默认值） | 验收 N-2 | 加 `batch` 未提供时 `max_ai_calls==0` 正向断言 |
| R-7 | 低 | 既有 config.py 与 default.yaml 存在默认值漂移（max_output_tokens 3000 vs 8000 等），T0.7 回归可能误判 | `config.py:162` vs `default.yaml:89` | A-4 明确基线（以 default.yaml 为准）并记录，避免把既有漂移当 T0.7 引入 |

## 3. 认可项

- 六段字段与方案 §5.5 草案逐一对齐；默认值安全（全部 enabled=False 或保守）；`batch` 0 语义与评审 §4.12 一致；`auto_promote=false`、`verify.fallback_to_single_turn_l2=true`、`report` 保守默认。
- 环境变量 `AI_APK_TRACER_EXPLORER__...` 与 `env_nested_delimiter="__"`/`case_sensitive=False` 兼容；`report` 段名无冲突。
- 三方同步策略、N-4（旧 yaml 用模型默认值，因 `get_settings` 不经 schema 校验）合理。
- 代码外发边界（仅方法级片段）、PoC 默认禁可执行、批量资源上限，均符合既有安全约定。

## 4. 处置记录（主 agent，第 1 轮）

| 编号 | 处置 | 理由 | 修订动作 | 修订后位置 |
|---|---|---|---|---|
| R-1 | 采纳 | 先声明后注册需显式归因，防硬解析 | §3.6 补 explorer 归因 T2.5 + 守卫说明 | 实施方案 §3.6 |
| R-2 | 采纳 | Path 解析助手成本低且正确 | 补 `resolved_assets_data_root()` + 测试 | 实施方案 §3.2/§3.5 |
| R-3 | 采纳 | schema 类型表达须一致 | §3.4 明说 string+minLength；测试断言 | 实施方案 §3.4 |
| R-4 | 部分采纳 | 采纳 description 层级区分；不改名（对齐草案） | 两处 description 写明层级 | 实施方案 §3.2 |
| R-5 | 采纳 | 防 schema/registry 漂移 | 补 N-5/N-6 + registry 一致性守卫测试 | 验收方案 §4/§3.5 |
| R-6 | 采纳 | 正反向配对更完整 | N-2 加正向断言 | 验收方案 §4 |
| R-7 | 采纳 | 明确基线防误判 | A-4 记录基线（以 default.yaml 为准） | 验收方案 §2 |

## 5. 开放决策项

无。7 项意见第 1 轮全部处置（6 采纳、1 部分采纳），进入实施。
