# P6 任务实施报告：E8 六项工程瑕疵批量清理

> **任务来源**：`docs/analysis/rules-review/2026-08-27-ruleset-quality-review.md` 第五节优先级 #6（E8 表六项，经审计 R-1/R-6 修订后的口径）
> **实施日期**：2026-08-27
> **实施者**：主 agent（GLM-5.3）

## 1. 变更清单

### E8-1：`ACTIVITY_SENSITIVE_NAME_HINT` 词表扩展（detector.py）

`(?:Reset|Password|Admin|Payment|Debug)` 5 词 → 13 词（补 `Login|Token|Account|Pay|Auth|Credential|Cert|Key`，与 `SENSITIVE_DATA_RE` 词表对齐）。规则为 auxiliary 加权信号，词表放宽只影响信号覆盖面（组件命名启发式）。

### E8-2：provider 无索引回退补 `LEGACY_INDEX_SCOPE` gap（detector.py execute）

`PROVIDER_FLOW_RULES` 在无索引时经 else 分支回退 `_component_rule` 旧式全文件逻辑——补 `flow_scope = {"gaps": [{"code": "LEGACY_INDEX_SCOPE", "critical": True}]}`（与 `COMPONENT_FLOW_ENTRIES` 无索引分支 528-532 行同口径），gaps 经 `_component_rule` 的 `scope_gaps` 链路（1977-1980 行）随候选上报——降级可观测。

### E8-3：全局规则同文件多调用点全枚举（detector.py `_webview_crypto_match`）

函数返回类型 `dict | None` → `list[dict]`：11 个规则分支全部改为 `finditer` 收集（JS_BRIDGE/FILE_ACCESS/UNIVERSAL/SSL/EXTERNAL_CONTENT/TRUST/VERIFIER/ECB/PENDING/LOG/HARDCODED）；`_global_code_rule` 调用点改 `for matched in ...` 逐命中产 L2 候选。旧行为只报每文件首个 match；EXTERNAL_CONTENT 的 js_enabled 与 loadUrl 组合语义保持（js_enabled 为前置条件）。测试 helper `_match` 兼容（取首个），新增 `_all_matches`。

### E8-4：DYNAMIC_RECEIVER 版本语义声明（rule.yaml limitations）

`RECEIVER_EXPORTED/RECEIVER_NOT_EXPORTED` 标志常量 API 33+ 引入、targetSdk 34+ 强制显式声明——limitations 补版本语义（不修改 `android_api: "1-36"` 数字——registerReceiver 本身 API 1 即有，改窄反而引入错误声明）。

### E8-5：severity 单源化（33 个 rule.yaml + 一致性测试）

批量从 `RULE_META` 注入 `severity` 字段到全部 33 个 rule.yaml（id 行后插入）；新增 `TestSeveritySingleSource::test_all_rule_yaml_severity_matches_rule_meta` 全量断言（yaml.severity == RULE_META.severity，防双源漂移）。

### E8-6：`GUARD_RE` 死代码清理（detector.py）

删除 `detector.py:105-108` 的 `GUARD_RE`（自认"当前无调用点"、与 `dataflow.GUARD_METHODS` 的同步注释同为摆设）——删除后全仓 grep 确认零代码引用（仅评审文档提及）。

## 2. 验证结果

- `test_webview_crypto_rules.py` 65/65（新增 TestSeveritySingleSource 1 + TestMultiMatchE8_3 3 + TestSensitiveNameHintE8_1 1 + 核验后 TestP6VerificationFixes 6）；
- 全量测试：**1295 passed / 0 failed**（40.07s，排除 `test_explorer.py`——3 个失败属并行会话 explorer 轨 WIP，见 P5 核验报告声明，与本任务无关）；
- 同步校验：PASS 82 / CONFLICT 0 / ORPHAN 0（不受影响）。

## 5. 核验处置修订（2026-08-27，deepseek-v4-pro 核验 R-1~R-7 后）

- **R-1（高，采纳（已实施））**：E8-2 的 gap 面市断链修复——通用尾部 `coverage_gaps` 合并 scope_gaps + 新增 `_attach_scope_gaps` helper 接入早 return 分支（NAME_HINT）；测试锚定 LEGACY_INDEX_SCOPE 到达候选；
- **R-2（中，采纳（已实施））**：5 个 sanitize 分支的 `text` 改按匹配跨度切原文（消除多命中文本错误与 AttributeError 崩溃路径），2 个测试锚定；
- **R-3（低，采纳（已实施））**：词表改驼峰/下划线/点分 token 整词匹配；`key` 移出词表（KeyEvent 拆词仍整词命中，误报面大于收益）；良性驼峰名负断言测试；
- **R-4（低，采纳（已实施））**：EXTERNAL_CONTENT 补注释排除 + js_enabled 前置 guard 重构 + 负断言测试；
- **R-5（低，采纳（已实施））**：两处 stale 注释更新；
- **R-6（低，采纳）**：测试计数更正（59→65，核验新增 6）；
- **R-7（低，采纳（已实施））**：severity 一致性测试补反向断言（防 RULE_META 死注册）。

核验后全量 **1295 passed / 0 failed**；核验报告：`2026-08-27-p6-e8-cleanup-audit.md`。

## 3. 行为影响评估

- E8-3 是**候选数量扩展**：同文件多调用点从 1 候选变 N 候选（JS_BRIDGE 旧版 artifact 已有 finditer 全枚举，行为对齐）——真实 APK 上 WebView/crypto/P5 规则候选数可能增加（如 shop run 的 WEAK_CIPHER_ECB 6 候选若集中在少数文件会变多）；
- E8-1 词表放宽增加 auxiliary 信号覆盖（Login/Token 命名的组件）；
- E8-2/E8-4/E8-5/E8-6 是可观测性/元数据修正，零候选语义变化。

## 4. 待核验点

1. E8-3 多命中后候选排序与去重（`_candidate_sort_key`）的行为；
2. E8-2 的 gap 是否真正到达候选的 blocking_gaps/coverage_gaps（scope_gaps 链路的终点）；
3. severity 注入后元数据测试的完备性（33 条全量断言）；
4. E8-1 词表中 `Key`/`Cert` 的误报面（KeyEvent/KeyEventDispatcher 类组件名）。
