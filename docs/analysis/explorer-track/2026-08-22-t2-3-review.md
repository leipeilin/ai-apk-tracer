# 评审文档：T2.3 实施方案与验收方案

> **任务编号**：T2.3
> **评审日期**：2026-08-22
> **评审对象**：`docs/analysis/explorer-track/2026-08-22-t2-3-implementation-plan.md`、`docs/analysis/explorer-track/2026-08-22-t2-3-acceptance-plan.md`
> **评审模型**：deepseek-v4-flash（独立子 agent，代码级核验）
> **评审者**：独立评审子 agent（code-explorer）
> **状态**：已闭合（第 1 轮处置完成，无开放决策项）

---

## 1. 评审结论摘要

方案与 T2.2 产物、T0.5 schema、detector 候选结构衔接基本正确，D1~D5 决策主干成立。两处中高：动态 receiver 的 unknown→False 与 D2 保守高估方向相反（manifest 侧高估、动态侧低估——语义分裂）；D3 与 T0.5 已交付样例（WEBVIEW_FILE_ACCESS_ENABLED 挂 activity）的矛盾未按 T2.2 评审 §6 先例显式勘误（全局规则按 D3 永不进组件能力，样例误导 Agent1 输入面预期）。修订后实施。

## 2. 问题清单

| 编号 | 严重度 | 问题 | 证据 | 修订建议 |
|---|---|---|---|---|
| R-1 | 中高 | 动态 exported None(unknown)→False 与 D2 "unknown 保守→True" 方向相反；N-4 未覆盖"静态 false+动态 None"；A-4 未断言 None 分支 | impl L71 vs L68/94 | 统一 None→True；N-4/A-4 补 None 分支 |
| R-2 | 中高 | D3 与 T0.5 样例张力未声明：WEBVIEW_FILE_ACCESS_ENABLED 属 GLOBAL_CODE_RULES（dynamic:path），按 D3 永不进组件能力，但 T0.5 已交付样例挂 activity；且非 bridge 的 WebView/crypto 能力对 Agent1（输入无 candidates）实际不可见 | detector.py:43,53-62,3440；test_rule_artifacts.py:380 | 比照 T2.2 评审 §6 勘误先例显式声明样例为草案理想态；全局能力可见性移交 T2.5 输入设计 |
| R-3 | 中 | entry_methods 限 manifest 条目致纯动态 receiver 恒空而 refs 非空；binder 方法归属未声明 | impl L59-60 | entry_methods=manifest+dynrcv 的 entry_method 去重集；binder 仅入 refs（非 lifecycle、格式异构） |
| R-4 | 中 | 测试手写 api_entry_table.json 层漂移风险（字段漂移时单测仍绿）；A-8 manifest-only 无 dynrcv/binder refs 端到端 | impl L108 | 夹具改"手写 rule-results + 真实 build_api_entry_table"；锚点明确复用 name(params)->return 格式 |
| R-5 | 中低 | 合并条目 exported=True 源于动态时 reason 仍为静态 reason（回溯误导）；动态判定字段透传未声明 | impl L72,95 | reason 组合标注 + 透传 export_status/externally_reachable |
| R-6 | 中低 | binder service_class 不在 manifest 时攻击面不可见（取舍未声明）；provider 读/写权限粒度丢失 | api_surface.py:179-181；manifest.py:143-146 | 声明取舍；provider 附加 read/write_permission 透传 |
| R-7 | 低 | 集成内联 20 行缺 `_stage` 两段式（T2.2 先例是独立方法）；auxiliary 候选聚合策略未声明 | impl L76-87；detector.py:63-65 | 封装方法 + `_stage`；声明 auxiliary 策略 |

## 3. 认可项

- D2 主干成立（双语义分层清晰）；D3 聚合键 component_name 正确（candidate["component"] 为规则族 kind，不可作键——代码实证）；D1/D4/D5 合理；candidates 时序可用；权限字段名核对一致；零 import rules 延续；边界检查全部通过。

## 4. 处置记录（主 agent，第 1 轮）

| 编号 | 处置 | 理由 | 修订动作 | 修订后位置 |
|---|---|---|---|---|
| R-1 | 采纳 | 语义分裂属实 | 动态 exported：显式 True→True / 显式 False→False / None→**True**（保守统一）；N-4/A-4 补 None 分支断言 | 实施稿 §3.2/§3.5、验收稿 N-4/A-4 |
| R-2 | 采纳 | 样例误导下游 | 勘误声明（T0.5 sample 的 sensitive_capabilities 为草案理想态——全局规则能力按 D3 不入组件）；全局能力可见性缺口记录移交 T2.5 输入设计（遗留观察项） | 实施稿 §3.4 D3 |
| R-3 | 采纳 | 信息损失 | entry_methods = manifest + dynrcv entry_method 去重集；binder 显式仅入 refs | 实施稿 §3.2 |
| R-4 | 采纳 | 漂移风险 | 测试夹具：手写 rule-results + **真实 build_api_entry_table 生成** entry_table；A-8 保持 manifest-only 但补 A-8b（source=true 轻量端到端？——不做（decompile 重），以单测真实生成器承载结构一致性；锚点注明格式） | 实施稿 §3.5 |
| R-5 | 采纳 | 回溯误导 | 合并条目 exported_reason = "static:{静态};dynamic:{动态判定}" 组合标注；export_status/externally_reachable 附加透传 | 实施稿 §3.2 |
| R-6 | 采纳 | 粒度/可见性 | 声明 binder 无挂靠取舍（api_entry_table 承载其可见性）；provider 附加 read_permission/write_permission 透传 | 实施稿 §3.2 |
| R-7 | 采纳 | 先例对齐 | 封装 `_generate_attack_surfaces`（_stage 两段式同 T2.2）；auxiliary 候选**含入**聚合（能力=全部规则命中，rule_id 自带 informational 语义可辨）——声明 | 实施稿 §3.3/§3.4 |

## 5. 开放决策项

无。7 项意见第 1 轮全部采纳，进入实施。

## 6. 遗留观察项（移交 T2.5）

- 非 bridge 的全局规则能力（WebView/crypto 族）对 Agent1 不可见（Agent1 输入=attack_surface+api_entry_table+call_tree，无 candidates）——T2.5 prompt/输入设计时评估：全局能力清单（按 rule_id 汇总）是否作为附加上下文注入。
