# 评审文档：T2.5a 实施方案与验收方案

> **任务编号**：T2.5a
> **评审日期**：2026-08-22
> **评审对象**：`docs/analysis/explorer-track/2026-08-22-t2-5a-implementation-plan.md`、`docs/analysis/explorer-track/2026-08-22-t2-5a-acceptance-plan.md`
> **评审模型**：deepseek-v4-flash（独立子 agent，代码级核验）
> **评审者**：独立评审子 agent（code-explorer）
> **状态**：已闭合（第 1 轮处置完成，无开放决策项）

---

## 1. 评审结论摘要

方案结构清晰、验收可操作，但存在**致命盲区（R-1）**：`ai_models.py` 中 T0.1 已交付 `ExplorerObservation/ExplorerLoopState/ReadRequest/ComponentSummary`（形状与方案不同：结构化 ComponentSummary、target/path/line 消歧设计、max_length=8、`_done_requires_chain` 校验器），`ai_explorer_observation.schema.json` 已提交注册——方案以"新增"口径会同名冲突或静默改写已提交 schema。D2 五操作与代码内评审 R-4 决策（resolve_invoke_target 不暴露）直接冲突。协议先行拆分方向正确，修订后实施。

## 2. 问题清单

| 编号 | 严重度 | 问题 | 证据 | 处置 |
|---|---|---|---|---|
| R-1 | 高 | 既有模型已存在且形状不同，方案"新增"口径冲突 | ai_models.py:307-352,691 | **采纳**：方案改"既有四模型零改动，仅新增 ExplorerInput"；§2 补既有模型清单（含四操作消歧设计与校验器语义） |
| R-2 | 高 | D2 五操作与既有评审 R-4 决策矛盾；"超集一致"属误标 | ai_models.py:310-315；方案 L235 | **采纳**：回退四操作（维持既有 ReadRequest 枚举零改动）；撤销"超集"表述 |
| R-3 | 高 | loop.done 语义自相矛盾（硬约束允许"无链可达"done=true vs 校验器拒绝） | ai_models.py:348-352 | **采纳**：维持校验器（done=true 必须伴随链）；"无链可达"不表达为 done=true——由驱动层预算终止承载；prompt 约束与校验器对齐；测试 3 回归锚定 |
| R-4 | 中 | method_id 应使用 MethodId（≤512）而非 ShortText | ai_models.py:43-49,281 | **采纳**（随 R-1 收窄：既有模型已用正确类型，本任务新模型不引入 method_id 字段——entry_json 文本承载） |
| R-5 | 中 | ExplorerEntryContext 投影错位（api_entry_table 无 method_id；未投影 exported 等） | api_entry_table.schema.json:14-44 | **采纳**：改 entry_json/attack_surface_json 文本注入（六类异构字段无损）；ComponentSummary.exported 为模型总结（低信任），T2.5b 转换层与 attack_surface 事实对照 |
| R-6 | 中 | prompt 缺仓库已固化防回归约束；既有必填/枚举回归测试不覆盖 explorer | test_prompt_registry.py:19-28,205-218 | **采纳**：§3.3 补约束清单（必填一个不省略/嵌套 required/行号≥1/枚举逐一/禁协议外字段）；测试 7 仿既有断言模式 |
| R-7 | 低 | "索引摘要"输入无对应字段；component_summary 结构化退化未论证 | 方案 L232 | **采纳**：attack_surface_json 承载"索引摘要"语义（归并说明）；既有结构化 ComponentSummary 不动（R-1） |
| R-8 | 低 | 拆分未同步实施计划 tracker | explorer-track-plan L74 | **采纳**：拆分记录于本评审文档（历史计划文档不回改）；T2.5b 方案将引用 |

## 3. 认可项

- A-6/A-7 机制引用准确（sync 手工登记+哈希生成；哈希门禁测试遍历全条目自动覆盖；`--write` 幂等）；D3 扁平文本与 DeepDive 先例一致；D4 loop 必答对齐 §4.3；预算字段与 ExplorerSettings 对齐；硬约束覆盖"不下结论/引用可回查"；A-5 所引 ChainProposal 双 schema 一致性已核实；边界检查通过（未越界 T2.5b/T2.6；AI_OUTPUT_MODEL_REGISTRY 随 T2.5b 加入——评审边界检查建议已记录为 T2.5b 前置项）。

## 4. 处置记录（主 agent，第 1 轮）

8 项意见全部采纳（R-1/R-2/R-3 为方案性修订：既有模型零改动 + 四操作维持 + 校验器语义对齐；R-4~R-8 随收窄范围消化）。方案 §2/§3.2/§3.3/§3.4/§3.5 已按处置修订。无开放决策项。

## 5. T2.5b 前置项记录（评审边界检查移交）

- `AI_OUTPUT_MODEL_REGISTRY` 运行时输出解析注册（ExplorerObservation）随 T2.5b 加入（config L188"注册前不得运行时解析"）；
- ExplorerInput → 驱动循环构造（entry_json 序列化 + 累积摘要策略）；
- 预算裁剪（read_requests 每轮 ≤8 vs 总预算 20——驱动层截断）。
