# 任务评审报告：M3-2

> **评审对象**：`2026-08-23-m3-2-implementation-plan.md`、`2026-08-23-m3-2-acceptance-plan.md`
> **评审日期**：2026-08-23
> **评审模型**：deepseek-v4-flash（独立只读子 agent）
> **状态**：第 1 轮已闭合（R-1~R-10 处置如下）

## 问题清单与处置记录

| 编号 | 严重度 | 问题摘要 | 处置 | 修订动作 |
|---|---|---|---|---|
| R-1 | 关键 | T3.2 种子字段（hypothesis/impact_proposal/component_summary）在 finding 数据中不存在——归一化层已丢弃（仅 severity_hint 保留）、component_summary 不在 ExplorerCandidate | **采纳 c（如实缓期）** | ReportInput 保留三可选字段（数据源存在时透传）；A-7 改负例断言（规则轨 finding 投影为 None）+ 大纲回写：种子真正兑现须先做归一化透传（记 M4 后续项）；R-3 处置改为"接口就绪、数据缓期"并在验收记录如实声明 |
| R-2 | 高 | RepairInput.target_output_model Literal 缺 "ReportDraftOutput"——repair 构造即 ValidationError 被吞 | **采纳** | 枚举增补；ai_repair_input.schema 哈希随 sync 变化；补 repair 正向用例 |
| R-3 | 高 | 漏注册 AI_OUTPUT_MODEL_REGISTRY/AI_OUTPUT_MODEL_VERSIONS——缓存描述符查表 KeyError 被吞 | **采纳** | 三 dict 一并注册；A-1 扩为三 dict 在册断言 |
| R-4 | 高 | routes 不传 analyzer——真协议生产 API 不可达 | **采纳** | routes 用 request.app.state.ai_runtime.create_analyzer() 接线（基建已备） |
| R-5 | 高 | provenance/prompt_version/model 传递机制未定义；evidence_refs 桥接来源未说明 | **采纳** | 分支逻辑：completed → ai_report_protocol + metadata 回填；evidence_refs 由确定性 _evidence_pointers 补齐（防 AI 虚构引用） |
| R-6 | 中 | deterministic_summary 拼接规则未定义；须排除 finding.description（L2 AI 文本） | **采纳** | 字段序（rule/component/sources/sinks/guard/auth/flow_kind/severity_hint）+ 10_000 截断 + 空兜底 + 单测 |
| R-7 | 中 | l2_review 投影字段名漂移（verdict vs candidate_verdict；harm 是嵌套对象） | **采纳** | 定 candidate_verdict/confidence_tier/flaw_holds + harm 结构化（impact_type/impact_target/server_confirmation_required） |
| R-8 | 中 | config 三方同步缺 default.yaml 与 config.schema.json | **采纳** | 变更清单补两文件 |
| R-9 | 中 | 降级可观测性缺失（失败分类去向未定义） | **采纳** | ai_draft.fallback 记录 classification+message；A-6 增断言 |
| R-10 | 低 | provider 优先级/_prompt_variable 惯例测试/缓存 no-op 声明三处遗漏 | **采纳** | analyzer 与 provider 双参时 provider 优先；A-3 补惯例测试；report_entry docstring 注明缓存 no-op（沿 verify_entry 先例） |

## 认可项（摘）

registry 结构/_invoke_prompt 签名/provider 衔接点锚点准确；严格契约前置直击 schema_invalid 根因；StrictAIModel 独立建模取舍合理；T0.9 先声明后注册已纳入；降级 fail-safe 成立。

## 闭合结论

R-1~R-10 全部采纳（R-1 取 c：接口就绪数据缓期——验收记录如实声明）。进入实施。
