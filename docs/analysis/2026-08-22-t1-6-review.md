# 评审文档：T1.6 测试覆盖核查报告

> **任务编号**：T1.6
> **评审日期**：2026-08-22
> **评审对象**：`docs/analysis/2026-08-22-t1-6-implementation-plan.md`（核查报告初版，结论"无新增代码"）
> **评审模型**：deepseek-v4-flash（独立子 agent）
> **评审者**：独立评审子 agent（code-explorer，逐条打开测试文件核验断言实质）
> **状态**：已闭合（第 1 轮处置完成——含高危缺陷修复）

---

## 1. 评审结论摘要

部分同意：迁移测试（L164）确已前置覆盖、无需新增；但"预算降级"（L163）存在**实质缺口 R-1（高危）**——降级"仅确定性主链"行为在真实 pipeline 层从未被验证，且经代码检查 run config 的 `ai.enabled=false` 在真实编排器中**无任何行为消费点**（orchestrator 仅读 config 的 source_analysis 段；AI 跳过仅由进程级 settings 控制）——降级疑似只落审计元数据而未真正跳过 AI，预算帽会被超耗。"无新增代码"结论不成立。

## 2. 问题清单

| 编号 | 严重度 | 问题描述 | 建议 |
|---|---|---|---|
| R-1 | 高 | 真实 pipeline 降级行为未验证且疑似未生效：run config ai 段仅 routes 展示消费；batch 降级只写元数据，真实 scan 仍按 settings.ai.enabled 执行 AI | 补单资产真实 ScanOrchestrator + 降级 config 用例；若失败即修 orchestrator 消费 run config ai 段 |
| R-2 | 中 | §2.1 手工证据与 T1.5 记录不符（A-6 为 total=1 而非 3 资产）——"3 资产真实链路覆盖（手工）"过度声明 | 补 3-APK 真实批次记录或如实降级表述 |
| R-3 | 中 | §2.4 测试名引用错误：`test_orchestrator_summary_requests_used` 不存在（实为 test_api.py 的 requests_used 断言）；正数计数路径仅 Fake 同构 | 更正引用，可选补正数断言 |
| R-4 | 低 | =1 字面用例缺失（=2 覆盖边界跨越，但 =1 的连续多降级序列未覆盖） | 一行参数成本，补 |
| R-5 | 低 | 失败重跑零集成证据 | T1.5 手工清单补一项 |

## 3. 核验明细（评审逐条实测）

- §2.1/§2.3/§2.4 的 Fake 层证据**全部属实**（full_flow 断言 3 run 关联列、并发峰值=1、标记列 [0,0,1]、分解计数）；
- §2.5 迁移矩阵**全部存在且断言到行级**（v1 legacy 数据完好含 finding id 改写、v3 叠加、中断幂等、半迁移、大表 1001 行、FK 需 PRAGMA ON、UNIQUE、新库）；
- §2.4 "仅主链复用 ai.enabled=false 既有机制"**断言不符**：test_pipeline_requires_ai 实测候选级 funnel 门控，与 run 级 ai.enabled 无关；全库无 ai.enabled=false 跑真实 pipeline 的测试。

## 4. 处置记录（主 agent，第 1 轮）

| 编号 | 处置 | 理由 | 修订动作 | 结果 |
|---|---|---|---|---|
| R-1 | 采纳（缺陷确认属实并修复） | 复核判断正确——`_run` 无 ai.enabled 消费（修复前实测） | orchestrator `_run` 读 config.ai.enabled（默认 True）→ `_run_ai_stage` 新增跳过路径（classification=disabled_by_run_config）；新增 `test_batch_real_pipeline_degradation`（3 资产真实 jadx + 墙钟降级：断言降级 run 的 ai_analysis=skipped + reason 含降级 + requests_used=0——修复前该断言必失败） | 修复 + 测试通过 |
| R-2 | 采纳 | 过度声明属实 | 新增真实测试即 3-APK 真实批次证据（同 R-1 用例）；报告 §2.1 表述更新 | 报告已更新 |
| R-3 | 采纳 | 引用错误属实 | 报告 §2.4 更正为 `test_upload_manifest_ai_summary_has_requests_used`；正数路径维持 Fake 同构 + 真实降级用例覆盖 =0 路径 | 报告已更新 |
| R-4 | 采纳 | 成本一行参数 | 新增 `test_run_batch_budget_degradation_cap_one`（=1：[0,1,1] 连续降级 + 批次继续） | 测试通过 |
| R-5 | 采纳 | 集成证据缺失 | T1.5 验收记录补录"失败资产子集重跑"手工项（见验收方案文档） | 已补录 |

## 5. 开放决策项

无。5 项意见第 1 轮全部采纳闭合。

## 6. 流程有效性印证

本任务的"独立复核"环节实际拦截了高危缺陷（R-1：设计文档宣称"复用 ai.enabled=false 行为"而实现层无消费点——文档与代码的脱节只有代码级复核能发现）。自查报告的"无缺口"初判与复核结论的偏差，验证了方案驱动流程中"子 agent 独立评审"环节的必要性。

## 7. 遗留观察项（记录，非缺陷）

- `_ai_requests_used` 正数路径（真实 AI 调用计数）依赖集成环境有 AI key——当前测试环境无 key，仅 Fake 同构覆盖（与 R-3 处置一致，接受）；
- 修复引入的 `ai_enabled` 参数默认 True 保持既有调用兼容（orchestrator 其余调用点无行为变化）。
