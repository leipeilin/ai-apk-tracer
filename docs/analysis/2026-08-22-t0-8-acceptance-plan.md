# 任务验收方案：T0.8（Asset/BatchScan 迁移设计）

> **任务编号**：T0.8
> **日期**：2026-08-22
> **依据实施方案**：`docs/analysis/2026-08-22-t0-8-implementation-plan.md`
> **状态**：起草
> **验收方式**：设计文档核对（本任务为设计稿，代码实现与测试归 T1.1）

---

## 1. 验收范围

- 迁移设计稿（数据模型 + 迁移 v4 + 升级路径 + 回滚 + 测试方案）完整且与现有 `repository.py` 迁移机制一致。

## 2. 验收点清单

| 编号 | 验收项 | 验收方式与命令/步骤 | 预期结果 |
|---|---|---|---|
| A-1 | 设计稿存在且含全部要素 | `2026-08-22-t0-8-implementation-plan.md` 存在；含表结构/版本/升级/回滚/测试五要素 | 通过 |
| A-2 | 迁移版本正确 | 设计稿 `DATABASE_SCHEMA_VERSION` 3→4、迁移函数名 `_migrate_assets_batches_v4` | 通过（与现有 v2/v3 风格一致） |
| A-3 | assets 表结构完整 | 字段/约束/索引与 §3.2 一致；`apk_sha256 UNIQUE` 防重 | 通过 |
| A-4 | batches 表结构完整 | 字段/状态枚举/预算快照/`ai_skipped_count` 与 §3.3 一致 | 通过 |
| A-5 | runs 关联列设计 | `asset_id`/`batch_id` 可空外键 `ON DELETE SET NULL` + `ai_skipped_by_batch_budget` 降级标记列（评审 R-2）；既有行 NULL/0 兼容；FK 恒开启声明（R-1） | 通过 |
| A-6 | 升级路径可判定 | 旧库→新库步骤（建表/加列/记录版本/user_version）逐条可执行 | 通过 |
| A-7 | 回滚策略明确 | 默认保留+重建；显式回滚 SQL 文档化（不自动执行） | 通过 |
| A-8 | 测试方案覆盖 | 升级/初始化/幂等/外键/SQL 安全 5 项（归 T1.1 执行） | 通过 |
| A-9 | 与 §4.13 一致 | 设计稿引用评审 §4.13/§4.12 并逐条对应 | 通过 |
| A-10 | SQL 安全 | 设计稿含参数绑定声明（无拼接）；迁移幂等 | 通过 |

## 3. 回归标准

- [ ] 本任务不触碰代码（仅设计稿）；现有测试全量通过不受影响。
- [ ] 设计稿与 `repository.py` 现状锚点（L15/L65/L131-150/L160 风格）一致，无虚构结构。

## 4. 边界与负例

| 编号 | 场景 | 设计稿处理 | 判定 |
|---|---|---|---|
| N-1 | 旧库（v3）迁移中断后重启 | `schema_migrations` 未记录 v4 → 幂等重跑（测试项 6 覆盖） | 设计稿覆盖 |
| N-2 | 删除 asset 时 run 的 asset_id | `ON DELETE SET NULL`（不级联删 run；FK 恒开启，`repository.py:54`） | 设计稿覆盖 |
| N-3 | batch 预算变更追溯 | 快照只读（创建时固化），不追溯历史 | 设计稿覆盖 |
| N-4 | 重复 apk 注册 | `apk_sha256 UNIQUE` 拒绝 | 设计稿覆盖 |
| N-5 | 叠加路径（v3 含 v2/v3 记录） | 复用 legacy 构造，测试项 6 覆盖（评审 R-4） | 设计稿覆盖 |
| N-6 | 大表迁移 | runs 大量行迁移正确（测试项 7；评审 R-3） | 设计稿覆盖 |
| N-7 | 回滚版本门槛 | 回滚前 `SELECT sqlite_version()` ≥3.35（评审 R-6） | 设计稿覆盖 |

## 5. 回退方案

- 设计稿验收不过：修订设计稿后复验；设计缺陷上升评审第 2 轮。

## 6. 验收记录（实施后填写）

> 实施后记录（2026-08-22）：T0.8 为**设计稿任务**（无代码），验收以设计文档完整性核对完成。评审 7 项意见第 1 轮全部采纳（含关键 R-2：`batches.ai_skipped_count` 聚合来源——runs 补 `ai_skipped_by_batch_budget` 列）。设计稿已修订，供 T1.1 落地。

| 编号 | 结果（通过/失败/部分通过） | 实测说明 | 复验结果 |
|---|---|---|---|
| A-1 | 通过 | 设计稿存在，含表结构/版本/升级/回滚/测试五要素 | - |
| A-2 | 通过 | `DATABASE_SCHEMA_VERSION` 3→4、`_migrate_assets_batches_v4` 与 v2/v3 同构 | - |
| A-3 | 通过 | assets 表：`apk_sha256 UNIQUE` 防重、`last_run_id` 可空外键、索引 | - |
| A-4 | 通过 | batches 表：状态枚举、预算快照、`ai_skipped_count` 聚合来源（runs 列） | - |
| A-5 | 通过 | runs 三新列（asset_id/batch_id 可空外键 + ai_skipped_by_batch_budget）；FK 恒开启（L54）；`_run_row` 兼容声明 | - |
| A-6 | 通过 | 升级路径逐条可执行；v4 仅建结构声明（R-7） | - |
| A-7 | 通过 | 回滚含 `DROP COLUMN` 三列 + 回滚前 `sqlite_version()` ≥3.35（R-6） | - |
| A-8 | 通过 | 测试方案 7 项（升级/初始化/幂等/外键/安全/叠加路径/大表） | - |
| A-9 | 通过 | 与评审 §4.13/§4.12 逐条对应 | - |
| A-10 | 通过 | 参数绑定声明 + 幂等设计 | - |
| N-1 | 通过 | 中断重跑（测试项 6） | - |
| N-2 | 通过 | ON DELETE SET NULL（FK 恒开启） | - |
| N-3 | 通过 | 快照只读不追溯 | - |
| N-4 | 通过 | apk_sha256 UNIQUE 防重 | - |
| N-5 | 通过 | 叠加路径（v3 含 v2/v3 记录，测试项 6） | - |
| N-6 | 通过 | 大表迁移（测试项 7） | - |
| N-7 | 通过 | 回滚版本门槛（sqlite_version） | - |
