# 评审文档：T2.4 实施方案与验收方案

> **任务编号**：T2.4
> **评审日期**：2026-08-22
> **评审对象**：`docs/analysis/2026-08-22-t2-4-implementation-plan.md`、`docs/analysis/2026-08-22-t2-4-acceptance-plan.md`
> **评审模型**：deepseek-v4-flash（独立子 agent，代码级核验）
> **评审者**：独立评审子 agent（code-explorer）
> **状态**：已闭合（第 1 轮处置完成，无开放决策项）

---

## 1. 评审结论摘要

两份方案整体扎实：D2 method_id 直通经代码核实成立（规则侧 `index_reader.py:813` 直接取 `methods.id` 列值——同库同列非字符串重建，无前缀转换问题）；七能力与 `get_call_relations_for_methods` 复用方式正确。主要缺陷：`build_bounded_tree` 丢弃歧义 gaps（数据零成本已带回却被静默丢弃，树"伪完整"与 D4 自相矛盾）；MAX_BODY_LINES=400 缺 token 预算论证（超既有 240 行上下文上限 67%）；环安全测试两文档不对齐。

## 2. 问题清单

| 编号 | 严重度 | 问题 | 证据 | 修订建议 |
|---|---|---|---|---|
| R-1 | 高 | build_bounded_tree 无 gaps 字段——歧义 gaps 被静默丢弃，树"伪完整"与 D4 矛盾 | index_store.py:622-641；实施稿 §3.2 | 树输出透传 gaps（T2.5 缺口清单消费） |
| R-2 | 中 | MAX_BODY_LINES=400 无预算论证（超 max_lines_per_context=240 的 67%；400 行 ≈12%-25% 输入预算） | config.py:161,170 | 对齐 240（同"进 prompt 行预算"语义） |
| R-3 | 中 | 环安全测试不对齐（验收 N-4 有、实施测试清单无） | 实施稿 §3.4 | 补 test_bounded_tree_cycle |
| R-4 | 低 | D2 锚点不准（method_id 构造在 L294 非 L327；格式含类前缀 `path#Class.method:line`；orchestrator L142 fallback 有 sources/ 前缀形态） | indexer.py:294,392 | 锚点修正 + 硬约束"method_id 列值直取、禁止按格式重建" |
| R-5 | 低 | 跨模块复用私有符号 `_resolve_lifecycle_methods` | api_surface.py:108 | 提升公共函数 |
| R-6 | 低 | node_limit 截断时 edges 端点 ∈ nodes 未约束 | 实施稿 §3.2 | 测试补断言 |
| R-7 | 低 | D5 降级未附替代动作提示（空入口表时 Agent1 应改用 search_symbol） | api_surface.py:209 | degraded 附建议文案 |

## 3. 认可项

- D2 核心链核实成立（列值直取）；get_call_relations 复用正确（批量/分片/单向）；class_hierarchy 双匹配必要（Java 简单名 vs smali FQCN 双形态是事实）；测试构造有先例（真实 index + resolved 跨文件调用）；CallTreeSettings 锚点准确；save_tree 原子写对齐先例；ai_models CodeReadRequest 四操作为七能力子集不冲突；边界检查全部通过（无 pipeline 改动/不越界 T2.5/artifacts 注册留 T2.10/零 import rules）。

## 4. 处置记录（主 agent，第 1 轮）

| 编号 | 处置 | 理由 | 修订动作 | 修订后位置 |
|---|---|---|---|---|
| R-1 | 采纳 | 伪完整与 D4 矛盾 | build_bounded_tree 输出加 `gaps` 字段（透传查询已带回的歧义 gap，按节点聚合） | 实施稿 §3.2 |
| R-2 | 采纳 | 预算一致性 | MAX_BODY_LINES=400→**240**（对齐 max_lines_per_context 同语义行预算；类常量保留，T2.5 实测可调） | 实施稿 §3.2 |
| R-3 | 采纳 | 文档对齐 | 补 test_bounded_tree_cycle（A→B→A 不死循环 + visited 实证） | 实施稿 §3.4 |
| R-4 | 采纳 | 锚点准确 | 锚点修 L294；D2 补硬约束"method_id 一律列值直取、禁止按格式重建"（前缀/类名形态差异由列值直取消除） | 实施稿 §2/§3.3 |
| R-5 | 采纳 | 依赖面显式 | api_surface 的 `_resolve_lifecycle_methods` 提升为公共 `resolve_component_lifecycle_methods`（私有名删除，内部调用同步） | 实施稿 §3.2 |
| R-6 | 采纳 | 结构一致性 | test_bounded_tree_limits 补"edges 端点 ⊆ nodes"断言 | 实施稿 §3.4 |
| R-7 | 采纳 | Agent 引导 | degraded 附建议动作文案（"入口表缺失：请用 search_symbol 定位组件类后以 lifecycle 方法为起点"） | 实施稿 §3.3 D5 |

## 5. 开放决策项

无。7 项意见第 1 轮全部采纳，进入实施。
