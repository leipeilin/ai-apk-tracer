# 双 APK 全量验收记录（第一次尝试——额度耗尽中止）

> **日期**：2026-08-23（19:23–21:40）
> **执行环境**：uvicorn 8178（**c3784a1 代码**——重启确认；AI_SURFACE/EXPLORER/VERIFY 三轨全开 + source_analysis）+ token-plan deepseek-v4-pro-0813
> **验收对象**：health `20260823T112336Z_2a80fc5a8735_34532ace`、shop `20260823T112340Z_1c55d3fb9f95_1390d665`（并行提交 19:23）
> **结果**：**中止（用户决策）**——token-plan 额度耗尽，429 空转 90 分钟后放弃；半成品 run 已清理。**验收延期至额度恢复后重跑（改单 run 串行）**。

## 1. 执行时间线（如实）

| 时间 | 事件 |
|---|---|
| 19:20 | 确认旧后端（04:26/04:59 启动）跑的是旧代码——**重启 uvicorn 加载 c3784a1**（三轨开关全开） |
| 19:23 | 并行提交双 run（health/shop） |
| 19:32 | basic_check/decompile/index/rule_prescan 顺利推进（shop decompile partial 出 614M + index 944M——与旧 run 一致） |
| 19:33–20:08 | 双 run 进入 explorer——**672 次 AI 请求成功**（双 run 合计；节奏与旧 run 相当） |
| 20:08 | **token-plan 额度耗尽**：429 持续（重试退避空转 2 次/分钟） |
| 20:32 | 确认死锁（672 成功数冻结、429 从 21 涨至 71→135→199） |
| 21:15–21:36 | 评估：自然走完需 4+ 小时（剩余入口 × 轮次 × retry 耗尽）；用户知情 |
| 21:40 | **用户决策放弃**：kill uvicorn（内存中的探索成果——批量落盘机制未到落盘时点——随进程丢失）；双 run 目录清理（~3GB 释放） |

## 2. 部分有效观察（429 前）

1. **新代码机械链路正常启动**：反编译/索引/规则预扫/探索阶段推进与旧 run 节奏一致（68 分钟 672 次成功请求——含双 run 并行限流拖累）；
2. **code_context 截断保护无崩溃**（全程零 error 日志——E-6 旁证）；
3. **内存健康**：uvicorn RSS 峰值 431MB（双 run 并行 + 探索密集期）——无泄漏迹象；
4. 429 占比 3% 前（21/672）retry 消化正常——限流问题仅在额度耗尽后出现。

## 3. 预期对照表（保留——重跑时使用）

见本文档 §1 前版本（E-1~E-11：verify fallback 大幅下降 / 探索三档上升 / D-3 零违规 / golden hit_rate ≥0.167 等——依据 dd52f12/9f364cd/7e5dc8b 修复链）。

## 4. 重跑计划（额度恢复后）

1. **单 run 串行**（避免双 run 并行限流——本次教训）；
2. 顺序：**先 shop**（验证 verify prompt 修复效果的价值最高）→ 后 health；
3. 执行环境同本次（c3784a1+ 三轨全开）；
4. 验收工具链就绪：`/tmp/dual-run-extract.py`（E-1~E-11 提取）+ `runner --runs` + `gate --baseline evaluation/baselines/m4-{health,shop}.json`；
5. 前置检查：额度恢复确认（单次冒烟请求 200）。

## 5. 教训记录

- **额度预算前置检查缺失**：验收前未评估当日已消耗额度（当天已跑 6 轮探索探针 + verify 探针 + M3-2 真实报告 + 双 run）——重跑前必须冒烟确认；
- **双 run 并行 = 限流风险翻倍**：单 run 串行是全量验收的稳妥默认；
- **探索批量落盘的脆弱性**（中途不可落盘）——改进方向（未来任务）：增量落盘/检查点机制（记为 backlog，非本次范围）。
