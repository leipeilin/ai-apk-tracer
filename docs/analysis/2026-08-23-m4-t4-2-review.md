# 任务评审报告：M4-T4.2

> **评审对象**：`2026-08-23-m4-t4-2-implementation-plan.md`、`acceptance-plan.md`
> **评审模型**：deepseek-v4-flash（独立只读子 agent）
> **状态**：第 1 轮已闭合（R-1~R-5 全部采纳）

| 编号 | 严重度 | 问题摘要 | 处置 | 修订 |
|---|---|---|---|---|
| R-1 | 高 | conditional_hit 计算路径不可实现（explorer_hit 对 conditional 恒 False） | 采纳 | conditional 命中经 `case.explorer_expected.matches(src, snk, hop_ids)` 直调（hop_ids 拼接逻辑复用 explorer_hit 的实现——提取为共享 helper） |
| R-2 | 高 | `--results` required=True 使 `--runs` 模式直接失败 | 采纳 | argparse mutually exclusive group（--results/--runs 二选一，均缺失报错）；补 N-4（同给/均不给 → 退出码 2） |
| R-3 | 中 | A-9 "hit_rate 预期 0" 理由错位（explorer_hit 消费全部 50 候选与 validated 无关） | 采纳 | 预期改"如实输出，以实际匹配断言" |
| R-4 | 中 | 聚合口径未指明 | 采纳 | 加权（总命中/总 hit case）；rate=None 的 run 剔除聚合并在结果中注明 unaggregated_runs |
| R-5 | 低 | RUNS_ROOT 常量不存在；阶段名为 ai_analysis | 采纳 | runner.py 定义 RUNS_ROOT（同 DEFAULT_MANIFEST 模式）；按名查 "ai_analysis"/"explorer"/"aggregation" |

认可项：数据锚点全部实读属实（chain_proposal 三字段/manifest stages 字段名/顶层时间）；explorer_hit 消费方式正确；指标口径与 T4.1 处置一致。
