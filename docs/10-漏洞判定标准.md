# 10 - 漏洞判定标准

> **更新日期**：2026-08-15
> **来源**：`doc/vuln-judgment-prompt-v1.md`（v1 提案）核心判定设计，已对照 l2-review 3.0.7 实现沉淀
> **用途**：人工复核、AI 判定校准、规则开发者理解"什么算漏洞"的共同标准
> **同步副本**：`.codebuddy/skills/apk-finding-review/references/vuln-judgment-criteria.md`（skill 内副本，供 fork 隔离环境引用）

---

## 1. 漏洞定义

> **漏洞 = 缺陷 + 可利用 + 产生危害**，并按可达性分级。四要素独立论证，缺一不可。

| 要素 | 判定问题 | 反例（不构成漏洞） |
|---|---|---|
| **缺陷成立** | 存在真实调用点的缺陷（非 import/注释/声明/共现） | 死代码、`HashMap.put` 仅内存组装、空 CRUD 返回 0 |
| **可利用** | 攻击者输入沿同值/同对象/key-slot 到达真实 Sink，**且执行结果有回到攻击者的通道** | 本地 Binder、LocalBroadcast、强签名 Guard、未注册组件；**Sink 执行但数据仅屏显、无跨进程外溢通道（红线 23）** |
| **产生危害** | 副作用超出攻击者既有能力，可具体描述 | 仅 UI 统计/CPS 字段、客户端回调但服务端 authoritative |
| **可达性分级** | 见 §7，不因非远程而一刀切否定 | 远程直达 vs 本地提权 vs 供应链投毒分开评级 |

**关键边界：Sink 执行 ≠ 影响外溢。** "可利用"必须同时证明：
- (a) 攻击者能触发 Sink 执行
- (b) 执行结果能以数据或状态副作用的形式**回到攻击者**或超出其既有能力

仅"在目标进程内执行并显示在用户屏幕上"，而数据无法跨进程取回（沙箱隔离、无 setResult、无 Accessibility/截屏可行通道），不构成可利用。

---

## 2. AI 结构化判定字段（l2-review 输出）

AI 对每个候选必须输出：

| 字段 | 类型 | 说明 |
|---|---|---|
| `flaw_holds` | bool + evidence_refs | 缺陷是否成立；引用必须是真实调用点 |
| `exploitability` | object | 入口/传播/Sink/Guard/授权逐项；传播必须同值/同对象/key-slot |
| `harm` | object | impact_type（数据泄露/篡改/DoS/提权/设备控制/资金）、impact_target、是否需要服务端/硬件确认 |
| `reachability_class` | enum | remote / local / supply_chain / device |
| `impact_vector` | object | CVSS 因子级描述：confidentiality/integrity/availability（none/partial/total）、privileges_required、attack_complexity、user_interaction |
| `reverse_exclusion` | array | 逐项对照 23 条红线，说明为何不算漏洞（或为何排除） |
| `confidence_rationale` | string | 为 confidence_tier 补充一句理由 |
| `refutation_basis` | enum array（可选） | **仅 `refutes_candidate` 时给出**；六值枚举（见 §5.1），每项必须能在输入 `candidate.deterministic_facts` 中找到对应事实；决策层逐项机器复核，任一项对不上则整体不予采信、退回人工 |

AI 判定依赖的**输入侧确定性事实**（`candidate.deterministic_facts`，规则引擎静态计算，可信度高于 AI 从代码窗口的推断，AI 不得用代码阅读推翻）：

| 事实字段 | 语义 |
|---|---|
| `value_flow_reaches_sink_argument` | 攻击者输入的值是否真正到达 Sink 实参；`false`（`flow_kind=control_to_sink`）表示 taint 引擎已确定无 untrusted 值到达 Sink 参数 |
| `deterministic_chain_verified` | 链路是否通过确定性验证（effect 已验证 + 摘要收敛 + 无 critical gap） |
| `sink_effect_verified` | 每个 Sink 的 `effect_verified`/`resolve_status`/`receiver_type` |
| `critical_gap_codes` | 规则已如实标注的 critical 缺口（静态限制，属"证据不足"，不得据此断言缺陷不成立） |
| `guard_status` / `authorization_status` / `operation_taxonomy` / `dataflow_status` | 同名语义，直接采信 |

**三条硬约束（铁律）**：
1. **AI 不输出 CVSS 分数**——只输出 `impact_vector` 因子级描述，分数由确定性映射器计算
2. **AI 不输出 severity_class**——定级唯一入口是 `determine_severity()`（AI 不定级铁律）
3. **confidence_tier 保持标量**——理由写独立字段 `confidence_rationale`

---

## 3. 反向排除（23 条固定红线清单）

AI 在给出 `supports_candidate` 前，必须逐项论证为什么没有命中以下红线（源自历史误报沉淀，可版本化更新）：

1. import/注释/字符串/声明命中，无真实调用点
2. 同文件/同组件共现，无同值传播
3. `HashMap.put` 仅内存组装，未进入敏感下游
4. 一般偏好写，key 仅统计/UI/CPS
5. `Executor.execute` receiver 是线程池
6. `stopSelf()` 仅生命周期结束
7. Provider mutation 返回 0/null/抛异常，未进真实 DB/File
8. 本地 Binder 无 descriptor/onTransact/AIDL Stub
9. LocalBroadcast/EventBus 进程内分发
10. 未注册 Activity（无 Manifest 声明且无插件/Instrumentation 接管）
11. `debuggable` 未知（默认 false）
12. 未选择实现（当前包/平台分支不会实例化）
13. 死代码（方法无调用点）
14. Receiver flag 误读（4=NOT_EXPORTED、sticky query、LocalBroadcast）
15. protected broadcast（普通应用不能发送该 action）
16. Manifest 权限遗漏（signature/knownSigner/internal/path permission/URI grant）
17. authority 冲突（实际解析对象未知）
18. 文件 mode 只读误判（268435456 = 只读）
19. 普通 `Class.forName` 未创建/挂载 Fragment
20. 客户端支付回调（服务端仍 authoritative）
21. 设备 API 调用只到应用状态机/返回码
22. shell UID 验证（仅 adb shell 成功 ≠ 普通应用 UID）
23. **Sink 执行但无影响外溢通道**：入口/Sink 均可达（数据流闭合），但执行结果仅屏显/进程内，攻击者无跨进程取回通道——链路全通但不可利用

---

## 4. 红线命中后的 verdict 映射（强制）

红线按证据性质分两类，映射固定（禁止 AI 随机选择）：

| 类别 | 判据 | 红线编号 | verdict |
|---|---|---|---|
| **确定性否定类** | 输入内确定性证据直接否定至少一个必要前提 | 3、5、6、7、8、9、13、14、15、18、19 | `refutes_candidate` |
| **证据缺失/需外部确认类** | 属于证据不足或需运行时/服务端确认，非确定性否定 | 1、2、4、10、11、12、16、17、20、21、22、23 | `unresolved` + `blocking_gap` |

**规则**：`refutes_candidate` 只能用于**确定性否定**。证据缺失一律 `unresolved` + `blocking_gap`（含 code、critical、closure）。**不得把"没找到证据"当作"证据表明不成立"。**

---

## 5. 红线 23 的静态阶段约束（防假阴性）

**风险**："无外溢通道"的判定本质上依赖动态验证（setResult 通道、Accessibility 可读性、跨进程取回、截屏门槛）。若允许 AI 静态推断并据此否决，会批量压掉真漏洞——**假阴性比误报危险得多**（误报会被人工发现，假阴性不会）。

**强制约束**：
- 红线 23 命中（外溢通道未验证）时**不得输出 `refutes_candidate`**（不能用"没找到外溢证据"充当否定依据）
- 必须生成 `blocking_gap`：`code=EXFILTRATION_CHANNEL_UNVERIFIED`、`critical=true`
- `closure` 写明待验证项；`exfiltration_channel` 输出 `unverified`；定级交由 `determine_severity()`（critical gap 存在时自动返回 pending）
- verdict 可为 `unresolved` 或 `supports_candidate`（后者须缺陷成立+入口可达+传播已证明，confidence 降级）
- **仅当存在静态确定性反证**（组件根本不返回数据、Sink 无任何输出通道且无持久化副作用，且须命中 §5.1 的 refutation_basis 六值之一并被 `candidate.deterministic_facts` 证实）时，才允许 `refutes_candidate`，并须给出反证 evidence_refs

**架构级兜底**（decision.py）：AI 输出 `refutes_candidate` 时**必须同时存在 `refutation_basis` 且每一项被 `deterministic_facts` 交叉验证通过**才采信为 `ai_false_positive`，否则落 `unresolved`（`_cross_validated_refutation_basis` 逐项复核，任一项对不上即整体不采信）。红线 23 约束因此是 prompt 层一致性要求（避免 AI 输出与决策层长期打架污染统计）。

### 5.1 静态可证例外（v3.0.7）：允许基于确定性事实的 refutes_candidate

红线 23 的"不得 refutes"针对的是**用"没找到外溢证据"充当否定依据**。若 `candidate.deterministic_facts` 已给出**正面的确定性反证**，则允许 `refutes_candidate`，但必须同时满足：

1. `verdict = refutes_candidate`；
2. `refutation_basis` 至少给出一项，且**每一项都能在 `deterministic_facts` 中找到对应事实**；
3. 为该反证给出可回查的 `evidence_refs`。

允许的 `refutation_basis` 取值及其**必须成立的事实前提**（决策层会逐项复核，对不上即整体不采信、退回人工）：

| basis | 事实前提 |
|---|---|
| `in_process_terminus` | `deterministic_facts.value_flow_reaches_sink_argument == false`（值流未到达 Sink 实参） |
| `guard_fail_closed` | `deterministic_facts.guard_status == "present_effective"` |
| `non_exported_provider` | `deterministic_facts.authorization_status` 为 protected/strongly_protected |
| `fixed_local_target` | 候选事实 `resolved_target_fixed == true` |
| `constant_sink_argument` | 候选事实 `sink_argument_constant == true` |
| `no_real_call_site` | 候选事实 `call_site_exists == false` |

**严禁**：编造 basis、给出无对应事实的 basis、或把"证据不足"（`critical_gap_codes` 中的静态限制）当作 basis。不确定时输出 `unresolved`——这不会被惩罚，编造会。

---

## 6. 反例案例：CommonBaseActivity 反射 Fragment（v03）

**来源**：`manual-verification-report/v03/v03-commonbaseactivity-report.md`（人工动态验证）

**自动声明链路（全部真实闭合）**：第三方应用 DexClassLoader 加载目标 APK → Parcel 构造 Parcelable → 外部 Intent 携带 `fragment_param` → exported 无权限 CommonBaseActivity → `Class.forName().newInstance()` 反射实例化任意 BaseFragment → 触发 Fragment 生命周期读取用户步数/心率/睡眠等本地数据。

**判定为误报的关键（动态验证证据）**：
- 数据确实在目标进程内被读取并**显示在用户屏幕上**（Fragment 成功打开）
- 但恶意应用**无法跨进程取回**：沙箱隔离、无 setResult 返回通道、Accessibility 不可行、截屏需授权
- 结论：第三方应用只能"让用户看到自己手机里的数据"，无法传给攻击者 → **不构成实际威胁 → 误报**

**对判定标准的教训**：
1. 数据流闭合（Source→Sink）只证明"缺陷成立 + 代码可达"，**不证明"可利用"**
2. "可利用"必须追问：**执行结果怎么回到攻击者？** 无外溢通道 = 影响不超出攻击者既有能力 = 危害不成立
3. 动态验证在此场景不可替代——**该结论不能反向作为静态否决依据**（见 §5）

---

## 7. 可达性分级

| 类别 | 定义 | 处理 |
|---|---|---|
| remote | 任意外部应用/远程可达入口 | 按最高影响评估，优先复核 |
| local | 需本地用户交互/本地提权前置条件 | 正常评估，注明前置条件与利用门槛 |
| supply_chain | 依赖/插件/资源投毒 | 单独标记，走供应链风险记录 |
| device | 设备 API 调用 | 分层：接口可调用 ≠ 硬件动作成功，需协议输出/前后状态确认 |

---

## 8. 红线清单的版本化与反馈闭环

- **版本化**：清单沉淀进提示词（当前 l2-review 3.0.7），每次校准 bump 版本，registry 精确解析，无隐式 fallback
- **反馈闭环**：人工复核 verdict → 沉淀为新误报模式 → 追加进红线清单；不自动修改模型，校准由使用者主动触发
- **联动防线**：参数化测试（`test_prompt_declares_every_schema_enum_value` 等）强制提示词声明 schema 全部 enum，防止红线/枚举漏声明

---

*本文档是漏洞判定标准的权威知识。代码逻辑优先于本文档；提示词（l2-review 3.0.7）、decision.py、severity.py 与本文档冲突时以代码为准并更新本文档。同步副本 `.codebuddy/skills/apk-finding-review/references/vuln-judgment-criteria.md` 由本文档镜像生成，随本文档更新。*
